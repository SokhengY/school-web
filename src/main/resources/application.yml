spring:
  application:
    name: school-web
  datasource:
    url: jdbc:postgresql://localhost:5432/school-web
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    org.hibernate: DEBUG